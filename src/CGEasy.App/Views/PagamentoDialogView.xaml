<Window x:Class="CGEasy.App.Views.PagamentoDialogView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Pagamento a Fornitore" 
        Height="650" Width="700"
        WindowStartupLocation="CenterOwner"
        Background="White">
    <Border Padding="25">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- HEADER -->
            <TextBlock Grid.Row="0" Text="📤 Pagamento a Fornitore" FontSize="20" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,20"/>

            <!-- FORM -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="15"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/> <!-- Nuova riga per campi fattura -->
                </Grid.RowDefinitions>

                <!-- COLONNA 1 -->
                <StackPanel Grid.Row="0" Grid.Column="0">
                    <TextBlock Text="Nome Fornitore *" Margin="0,0,0,5" FontWeight="SemiBold"/>
                    <TextBox Text="{Binding NomeFornitore, UpdateSourceTrigger=PropertyChanged}" Padding="8" Margin="0,0,0,15"/>
                </StackPanel>

                <StackPanel Grid.Row="1" Grid.Column="0">
                    <TextBlock Text="Mese (1-12)" Margin="0,0,0,5" FontWeight="SemiBold"/>
                    <TextBox Text="{Binding Mese, UpdateSourceTrigger=PropertyChanged}" Padding="8" Margin="0,0,0,15"/>
                </StackPanel>

                <StackPanel Grid.Row="2" Grid.Column="0">
                    <TextBlock Text="Data Scadenza *" Margin="0,0,0,5" FontWeight="SemiBold"/>
                    <DatePicker SelectedDate="{Binding DataScadenza}" Padding="8" Margin="0,0,0,15"/>
                </StackPanel>

                <StackPanel Grid.Row="2" Grid.Column="2">
                    <TextBlock Text="Data Pagamento Effettivo" Margin="0,0,0,5" FontWeight="SemiBold"/>
                    <DatePicker SelectedDate="{Binding DataPagamentoEffettivo}" Padding="8" Margin="0,0,0,15"/>
                </StackPanel>

                <!-- COLONNA 2 -->
                <StackPanel Grid.Row="0" Grid.Column="2">
                    <TextBlock Text="Anno" Margin="0,0,0,5" FontWeight="SemiBold"/>
                    <TextBox Text="{Binding Anno, UpdateSourceTrigger=PropertyChanged}" Padding="8" Margin="0,0,0,15"/>
                </StackPanel>

                <StackPanel Grid.Row="1" Grid.Column="2">
                    <TextBlock Text="Importo (€) *" Margin="0,0,0,5" FontWeight="SemiBold"/>
                    <TextBox Text="{Binding Importo, UpdateSourceTrigger=PropertyChanged}" Padding="8" Margin="0,0,0,15"/>
                </StackPanel>

                <!-- CAMPI FATTURA FORNITORE -->
                <StackPanel Grid.Row="3" Grid.Column="0">
                    <TextBlock Text="Numero Fattura Fornitore" Margin="0,0,0,5" FontWeight="SemiBold"/>
                    <TextBox Text="{Binding NumeroFatturaFornitore, UpdateSourceTrigger=PropertyChanged}" Padding="8" Margin="0,0,0,15"/>
                </StackPanel>

                <StackPanel Grid.Row="3" Grid.Column="2">
                    <TextBlock Text="Data Fattura Fornitore" Margin="0,0,0,5" FontWeight="SemiBold"/>
                    <DatePicker SelectedDate="{Binding DataFatturaFornitore}" Padding="8" Margin="0,0,0,15"/>
                </StackPanel>

                <!-- CHECKBOX PAGATO -->
                <StackPanel Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <CheckBox IsChecked="{Binding Pagato, UpdateSourceTrigger=PropertyChanged}" 
                              VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <TextBlock Text="Pagato" FontWeight="SemiBold" VerticalAlignment="Center"/>
                </StackPanel>

                <!-- NOTE (FULL WIDTH) -->
                <StackPanel Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="3">
                    <TextBlock Text="Note" Margin="0,5,0,5" FontWeight="SemiBold"/>
                    <TextBox Text="{Binding Note, UpdateSourceTrigger=PropertyChanged}" 
                             Padding="8" Height="60" TextWrapping="Wrap" AcceptsReturn="True" 
                             VerticalScrollBarVisibility="Auto" FontSize="13"/>
                </StackPanel>
            </Grid>

            <!-- PULSANTI -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                <Button Content="💾 Salva" Command="{Binding SalvaCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
                        Padding="20,10" Margin="0,0,10,0" Background="#27AE60" Foreground="White" BorderThickness="0" Cursor="Hand"/>
                <Button Content="❌ Annulla" Command="{Binding AnnullaCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
                        Padding="20,10" Background="#95A5A6" Foreground="White" BorderThickness="0" Cursor="Hand"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>

