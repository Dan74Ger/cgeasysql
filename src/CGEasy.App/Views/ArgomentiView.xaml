<UserControl x:Class="CGEasy.App.Views.ArgomentiView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1200">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- HEADER -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="📂 Argomenti Circolari" 
                          FontSize="28" 
                          FontWeight="Bold" 
                          Foreground="#2c3e50"/>
                <TextBlock Text="Gestisci le categorie per classificare le circolari" 
                          FontSize="14" 
                          Foreground="#7f8c8d" 
                          Margin="0,5,0,0"/>
            </StackPanel>

            <Button Grid.Column="1" 
                   Content="➕ Nuovo Argomento" 
                   Command="{Binding NuovoArgomentoCommand}"
                   Background="#27ae60" 
                   Foreground="White" 
                   Padding="20,12" 
                   FontSize="14" 
                   FontWeight="SemiBold"
                   BorderThickness="0"
                   Cursor="Hand"
                   VerticalAlignment="Center">
                <Button.Style>
                    <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                        <Setter Property="Background" Value="#27ae60"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#229954"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>

        <!-- PANEL MODIFICA/NUOVO (visibile quando IsEditing = true) -->
        <Border Grid.Row="1" 
                Visibility="{Binding IsEditing, Converter={StaticResource BooleanToVisibilityConverter}}"
                Background="#f8f9fa" 
                BorderBrush="#dee2e6" 
                BorderThickness="1" 
                CornerRadius="8" 
                Padding="20" 
                Margin="0,0,0,15">
            <StackPanel>
                <TextBlock Text="✏️ Dettagli Argomento" 
                          FontSize="18" 
                          FontWeight="SemiBold" 
                          Foreground="#2c3e50" 
                          Margin="0,0,0,15"/>

                <!-- Nome Argomento -->
                <TextBlock Text="Nome Argomento *" 
                          FontSize="13" 
                          FontWeight="SemiBold" 
                          Foreground="#495057" 
                          Margin="0,0,0,5"/>
                <TextBox Text="{Binding NomeArgomento, UpdateSourceTrigger=PropertyChanged}" 
                        FontSize="14" 
                        Padding="10" 
                        Margin="0,0,0,15"
                        MaxLength="100"/>

                <!-- Descrizione (opzionale) -->
                <TextBlock Text="Descrizione (opzionale)" 
                          FontSize="13" 
                          FontWeight="SemiBold" 
                          Foreground="#495057" 
                          Margin="0,0,0,5"/>
                <TextBox Text="{Binding DescrizioneArgomento, UpdateSourceTrigger=PropertyChanged}" 
                        FontSize="14" 
                        Padding="10" 
                        Height="80"
                        TextWrapping="Wrap"
                        AcceptsReturn="True"
                        VerticalScrollBarVisibility="Auto"
                        Margin="0,0,0,15"
                        MaxLength="500"/>

                <!-- Pulsanti -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="💾 Salva" 
                           Command="{Binding SalvaArgomentoCommand}"
                           Background="#27ae60" 
                           Foreground="White" 
                           Padding="20,10" 
                           FontSize="13" 
                           FontWeight="SemiBold"
                           BorderThickness="0"
                           Margin="0,0,10,0"
                           Cursor="Hand">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                                <Setter Property="Background" Value="#27ae60"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#229954"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>

                    <Button Content="✖️ Annulla" 
                           Command="{Binding AnnullaModificaCommand}"
                           Background="#95a5a6" 
                           Foreground="White" 
                           Padding="20,10" 
                           FontSize="13" 
                           FontWeight="SemiBold"
                           BorderThickness="0"
                           Cursor="Hand">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                                <Setter Property="Background" Value="#95a5a6"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#7f8c8d"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- LISTA ARGOMENTI -->
        <Border Grid.Row="2" 
                Background="White" 
                BorderBrush="#dee2e6" 
                BorderThickness="1" 
                CornerRadius="8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Barra ricerca -->
                <Border Grid.Row="0" 
                       Background="#f8f9fa" 
                       BorderBrush="#dee2e6" 
                       BorderThickness="0,0,0,1" 
                       Padding="15">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBox Grid.Column="0" 
                                Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
                                FontSize="14" 
                                Padding="10"
                                VerticalContentAlignment="Center"
                                materialDesign:HintAssist.Hint="🔍 Cerca argomento..."/>

                        <Button Grid.Column="1" 
                               Content="Cerca" 
                               Command="{Binding SearchCommand}"
                               Background="#3498db" 
                               Foreground="White" 
                               Padding="20,8" 
                               Margin="10,0,0,0"
                               FontSize="13" 
                               BorderThickness="0"
                               Cursor="Hand">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                                    <Setter Property="Background" Value="#3498db"/>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#2980b9"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </Grid>
                </Border>

                <!-- DataGrid -->
                <DataGrid Grid.Row="1" 
                         ItemsSource="{Binding Argomenti}" 
                         SelectedItem="{Binding ArgomentoSelezionato}"
                         AutoGenerateColumns="False" 
                         IsReadOnly="True"
                         CanUserAddRows="False"
                         CanUserDeleteRows="False"
                         CanUserResizeRows="False"
                         SelectionMode="Single"
                         GridLinesVisibility="Horizontal"
                         HeadersVisibility="Column"
                         RowHeight="50"
                         Background="White"
                         BorderThickness="0"
                         Margin="0">

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="80"/>
                        <DataGridTextColumn Header="Nome Argomento" Binding="{Binding Nome}" Width="250"/>
                        <DataGridTextColumn Header="Descrizione" Binding="{Binding Descrizione}" Width="*"/>
                        <DataGridTextColumn Header="Data Creazione" Binding="{Binding DataCreazione, StringFormat='dd/MM/yyyy HH:mm'}" Width="180"/>

                        <!-- Colonna Azioni -->
                        <DataGridTemplateColumn Header="Azioni" Width="200">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <Button Content="✏️" 
                                               Command="{Binding DataContext.ModificaArgomentoCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                               Background="#f39c12" 
                                               Foreground="White" 
                                               Width="35" 
                                               Height="35"
                                               FontSize="14"
                                               BorderThickness="0"
                                               Margin="5,0"
                                               Cursor="Hand"
                                               ToolTip="Modifica">
                                            <Button.Style>
                                                <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                                                    <Setter Property="Background" Value="#f39c12"/>
                                                    <Style.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="Background" Value="#e67e22"/>
                                                        </Trigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                        </Button>

                                        <Button Content="🗑️" 
                                               Command="{Binding DataContext.EliminaArgomentoCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                               Background="#e74c3c" 
                                               Foreground="White" 
                                               Width="35" 
                                               Height="35"
                                               FontSize="14"
                                               BorderThickness="0"
                                               Margin="5,0"
                                               Cursor="Hand"
                                               ToolTip="Elimina">
                                            <Button.Style>
                                                <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                                                    <Setter Property="Background" Value="#e74c3c"/>
                                                    <Style.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="Background" Value="#c0392b"/>
                                                        </Trigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                        </Button>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>

