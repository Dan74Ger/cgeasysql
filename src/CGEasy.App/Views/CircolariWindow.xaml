<Window x:Class="CGEasy.App.Views.CircolariWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:views="clr-namespace:CGEasy.App.Views"
        mc:Ignorable="d"
        Title="📄 Gestione Circolari"
        Height="900"
        Width="1400"
        WindowStartupLocation="CenterScreen"
        Background="#ecf0f1">

    <TabControl Margin="15" 
                Background="White" 
                BorderBrush="#dee2e6" 
                BorderThickness="1"
                SelectedIndex="2">
        <TabControl.Resources>
            <Style TargetType="TabItem">
                <Setter Property="Padding" Value="20,12"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Background" Value="#f8f9fa"/>
                <Setter Property="Foreground" Value="#495057"/>
                <Setter Property="Margin" Value="0,0,3,0"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="TabItem">
                            <Border Name="Border" 
                                   Background="{TemplateBinding Background}"
                                   BorderBrush="#dee2e6"
                                   BorderThickness="1,1,1,0"
                                   CornerRadius="8,8,0,0"
                                   Padding="{TemplateBinding Padding}"
                                   Margin="{TemplateBinding Margin}">
                                <ContentPresenter ContentSource="Header" 
                                                 HorizontalAlignment="Center" 
                                                 VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter TargetName="Border" Property="Background" Value="White"/>
                                    <Setter Property="Foreground" Value="#2c3e50"/>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Border" Property="Background" Value="#e9ecef"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </TabControl.Resources>

        <!-- Tab Argomenti -->
        <TabItem Header="📂 Argomenti">
            <views:ArgomentiView/>
        </TabItem>

        <!-- Tab Importa Circolare -->
        <TabItem Header="📤 Importa Circolare">
            <views:ImportaCircolareView/>
        </TabItem>

        <!-- Tab Ricerca Circolari -->
        <TabItem Header="🔍 Ricerca">
            <views:RicercaCircolariView/>
        </TabItem>
    </TabControl>
</Window>

