<Window x:Class="CGEasy.App.Views.InputDialogView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Input" 
        Width="600" Height="220"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="SingleBorderWindow">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" x:Name="PromptTextBlock" TextWrapping="Wrap" Margin="0,0,0,15" FontSize="13"/>
        
        <TextBox Grid.Row="1" x:Name="ValueTextBox" VerticalContentAlignment="Center" Padding="10" FontSize="13"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="OK" Width="100" Height="35" Margin="0,0,10,0" IsDefault="True" Click="OkButton_Click"/>
            <Button Content="Annulla" Width="100" Height="35" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>

