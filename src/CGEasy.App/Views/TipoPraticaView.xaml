<UserControl x:Class="CGEasy.App.Views.TipoPraticaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="http://schemas.modernwpf.com/2019"
             xmlns:models="clr-namespace:CGEasy.Core.Models;assembly=CGEasy.Core"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000"
             Background="{DynamicResource SystemControlPageBackgroundChromeLowBrush}">

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Statistics Cards -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="White" CornerRadius="8" Padding="20" Margin="0,0,10,0"
                    ui:ThemeManager.HasThemeResources="True">
                <ui:ThemeManager.RequestedTheme><ui:ElementTheme>Light</ui:ElementTheme></ui:ThemeManager.RequestedTheme>
                <StackPanel>
                    <TextBlock Text="📋" FontSize="24" Margin="0,0,0,5"/>
                    <TextBlock Text="{Binding TotalPratiche}" FontSize="32" FontWeight="Bold" Foreground="#007ACC"/>
                    <TextBlock Text="Totali" FontSize="13" Foreground="Gray"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="1" Background="White" CornerRadius="8" Padding="20" Margin="0,0,10,0"
                    ui:ThemeManager.HasThemeResources="True">
                <ui:ThemeManager.RequestedTheme><ui:ElementTheme>Light</ui:ElementTheme></ui:ThemeManager.RequestedTheme>
                <StackPanel>
                    <TextBlock Text="✅" FontSize="24" Margin="0,0,0,5"/>
                    <TextBlock Text="{Binding PraticheAttive}" FontSize="32" FontWeight="Bold" Foreground="#00B294"/>
                    <TextBlock Text="Attive" FontSize="13" Foreground="Gray"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Background="White" CornerRadius="8" Padding="20"
                    ui:ThemeManager.HasThemeResources="True">
                <ui:ThemeManager.RequestedTheme><ui:ElementTheme>Light</ui:ElementTheme></ui:ThemeManager.RequestedTheme>
                <StackPanel>
                    <TextBlock Text="❌" FontSize="24" Margin="0,0,0,5"/>
                    <TextBlock Text="{Binding PraticheInattive}" FontSize="32" FontWeight="Bold" Foreground="#E81123"/>
                    <TextBlock Text="Inattive" FontSize="13" Foreground="Gray"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Toolbar -->
        <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="20,15" Margin="0,0,0,15"
                ui:ThemeManager.HasThemeResources="True">
            <ui:ThemeManager.RequestedTheme><ui:ElementTheme>Light</ui:ElementTheme></ui:ThemeManager.RequestedTheme>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                             ui:ControlHelper.PlaceholderText="🔍 Cerca pratica..."
                             Width="220" Height="36" VerticalContentAlignment="Center" Margin="0,0,10,0"/>
                    
                    <CheckBox Content="Solo Attive" IsChecked="{Binding ShowOnlyActive}"
                              VerticalAlignment="Center" Margin="0,0,10,0"/>
                    
                    <Button Content="🔄 Aggiorna" Command="{Binding RefreshCommand}"
                            Height="36" Padding="15,0" Margin="0,0,10,0"/>
                    
                    <Button Content="🗑️ Pulisci Filtri" Command="{Binding ClearFiltersCommand}"
                            Height="36" Padding="15,0"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="➕ Nuova Pratica" Command="{Binding NewPraticaCommand}"
                            Style="{StaticResource AccentButtonStyle}" Height="36" Padding="20,0"
                            FontWeight="SemiBold" Margin="0,0,10,0"/>
                    <Button Content="📊 Export Excel" Command="{Binding ExportExcelCommand}"
                            Height="36" Padding="15,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Data Grid -->
        <Border Grid.Row="2" Background="White" CornerRadius="8" Padding="0"
                ui:ThemeManager.HasThemeResources="True">
            <ui:ThemeManager.RequestedTheme><ui:ElementTheme>Light</ui:ElementTheme></ui:ThemeManager.RequestedTheme>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <DataGrid Grid.Row="0" ItemsSource="{Binding Pratiche}"
                          SelectedItem="{Binding SelectedPratica}"
                          AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single"
                          CanUserSortColumns="True" CanUserReorderColumns="True" CanUserResizeColumns="True"
                          GridLinesVisibility="Horizontal" HeadersVisibility="Column"
                          AlternatingRowBackground="#F9F9F9" Margin="1">

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Ord." Binding="{Binding Ordine}" Width="80">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                    <Setter Property="HorizontalAlignment" Value="Center"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTextColumn Header="Stato" Binding="{Binding Attivo}" Width="70">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                    <Setter Property="Text" Value="❌"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Attivo}" Value="True">
                                            <Setter Property="Text" Value="✅"/>
                                            <Setter Property="Foreground" Value="Green"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Attivo}" Value="False">
                                            <Setter Property="Foreground" Value="Red"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTextColumn Header="Nome Pratica" Binding="{Binding NomePratica}" Width="*" MinWidth="250">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                    <Setter Property="Padding" Value="5,0"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTextColumn Header="Descrizione" Binding="{Binding Descrizione}" Width="*" MinWidth="300"/>
                    </DataGrid.Columns>

                    <DataGrid.ContextMenu>
                        <ContextMenu>
                            <MenuItem Header="📝 Modifica" Command="{Binding EditPraticaCommand}"/>
                            <MenuItem Header="👁 Visualizza Dettagli" Command="{Binding ViewDetailsCommand}"/>
                            <Separator/>
                            <MenuItem Header="✅ Attiva" Command="{Binding ActivatePraticaCommand}"/>
                            <MenuItem Header="❌ Disattiva" Command="{Binding DeactivatePraticaCommand}"/>
                            <Separator/>
                            <MenuItem Header="🗑️ Elimina Definitivamente" 
                                      Command="{Binding DeletePermanentlyCommand}"
                                      Foreground="Red"
                                      FontWeight="Bold"/>
                        </ContextMenu>
                    </DataGrid.ContextMenu>
                </DataGrid>

                <Border Grid.Row="1" BorderBrush="#E0E0E0" BorderThickness="0,1,0,0"
                        Background="#F5F5F5" Padding="20,15">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="📝 Modifica" Command="{Binding EditPraticaCommand}"
                                Height="36" Width="110" Margin="0,0,10,0"/>
                        <Button Content="👁 Dettagli" Command="{Binding ViewDetailsCommand}"
                                Height="36" Width="100" Margin="0,0,10,0"/>
                        <Button Content="✅ Attiva" Command="{Binding ActivatePraticaCommand}"
                                Height="36" Width="100" Margin="0,0,10,0"/>
                        <Button Content="❌ Disattiva" Command="{Binding DeactivatePraticaCommand}"
                                Height="36" Width="100" Margin="0,0,10,0"/>
                        <Button Content="🗑️ Elimina" 
                                Command="{Binding DeletePermanentlyCommand}"
                                Height="36"
                                Width="100"
                                Background="#E81123"
                                Foreground="White"
                                FontWeight="Bold"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>
    </Grid>
</UserControl>
